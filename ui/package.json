{
  "name": "snow-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Snow UI — debug console and memory inspector for Snowclaw collective memory",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "nostr-tools": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
